coverage:
  status:
    project:
      default:
        # Informational only - don't block PRs on overall coverage changes.
        # Stale baselines cause false failures on dependency-only PRs.
        informational: true
        threshold: 2%
    patch:
      default:
        target: 90%
